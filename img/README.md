Drop images here, with the following directory structure:

    <YEAR>/<MONTH>/<IMAGE>

where `IMAGE` is, in return:

    <YEAR><MONTH><DAY>.jpeg

Also, place a `404.jpeg` for cases when an image is unavailable.
